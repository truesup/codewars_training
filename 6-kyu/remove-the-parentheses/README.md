<h2><a href=https://www.codewars.com/kata/5f7c38eb54307c002a2b8cc8/train/javascript target="_blank">Remove the parentheses</a></h2><h3>6 kyu</h3><h1 id="remove-the-parentheses">Remove the parentheses</h1><p>In this kata you are given a string for example:</p><pre><code class="language-python"><span class="cm-string">"example(unwanted thing)example"</span></code></pre><p>Your task is to remove everything inside the parentheses as well as the parentheses themselves.</p><p>The example above would return:</p><pre><code class="language-python"><span class="cm-string">"exampleexample"</span></code></pre><h2 id="notes">Notes</h2><ul><li>Other than parentheses only letters and spaces can occur in the string. Don't worry about other brackets like <code>"[]"</code> and <code>"{}"</code> as these will never appear.</li><li>There can be multiple parentheses.</li><li>The parentheses can be nested.</li></ul>