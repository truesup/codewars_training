<h2><a href=https://www.codewars.com/kata/5631213916d70a0979000066/train/javascript target="_blank">Number-Star ladder</a></h2><h3>7 kyu</h3><p>Using <code>n</code> as a parameter in the function <code>pattern</code>, where <code>n &gt; 0</code>, complete the code to get the pattern:</p><pre><code>11*21**31***4... and so on...</code></pre><p><strong>Note:</strong> There is no newline in the end (after the pattern ends).</p><h3 id="examples">Examples</h3><p><code>pattern(3)</code> should return <code>"1\n1*2\n1**3"</code>, e.g. the following:</p><pre><code>11*21**3</code></pre><p><code>pattern(10):</code> should return the following:</p><pre><code>11*21**31***41****51*****61******71*******81********91*********10</code></pre>