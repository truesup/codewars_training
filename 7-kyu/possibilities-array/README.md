<h2><a href=https://www.codewars.com/kata/59b710ed70a3b7dd8f000027/train/javascript target="_blank">Possibilities Array</a></h2><h3>7 kyu</h3><p>A non-empty array <code>a</code> of length <code>n</code> is called an array of all possibilities if it contains all numbers between <code>0</code> and <code>a.length - 1</code> (both inclusive).</p><p>Write a function that accepts an integer array and returns <code>true</code> if the array is an array of all possibilities, else <code>false</code>.</p><p>Examples:</p><pre><code class="language-python">[<span class="cm-number">1</span>,<span class="cm-number">2</span>,<span class="cm-number">0</span>,<span class="cm-number">3</span>] <span class="cm-operator">=&gt;</span> <span class="cm-keyword">True</span><span class="cm-comment"># Includes all numbers between 0 and a.length - 1 (4 - 1 = 3)</span>[<span class="cm-number">0</span>,<span class="cm-number">1</span>,<span class="cm-number">2</span>,<span class="cm-number">2</span>,<span class="cm-number">3</span>] <span class="cm-operator">=&gt;</span> <span class="cm-keyword">False</span><span class="cm-comment"># Doesn't include all numbers between 0 and a.length - 1 (5 - 1 = 4)</span>[<span class="cm-number">0</span>] <span class="cm-operator">=&gt;</span> <span class="cm-keyword">True</span><span class="cm-comment"># Includes all numbers between 0 and a.length - 1 (1 - 1 = 0).</span></code></pre>